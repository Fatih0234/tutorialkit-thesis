from helpers import greet
from template_helper import template_suffix

name = "Ada"
print(greet(name) + template_suffix())

# Uncomment to inspect a traceback:
# raise ValueError("intentional lesson error")
